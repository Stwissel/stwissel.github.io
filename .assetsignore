**/.git
**/node_modules
**/.DS_Store
**/.wrangler
wrangler.jsonc
worker

# Exceeds the 25 MiB per-asset limit. Served from origin.wissel.net via
# _redirects instead. Listed here so a deploy still succeeds even if CI
# re-adds the file before the blogsource rsync exclude is in place.
blog/images/2019/invideo-lwc-images.mp4
